-
  const gym = {
    title: `
      ТРЕНАЖЕРНИЙ ЗАЛ
    `,
    description: `
      Найкраще розпочинати або завершувати день у тренажерному залі. Сучасне обладнання підходить як для новачків, так і для професійних спортсменів. Просторий зал, роздягальня та душ зроблять відвідування залу ще приємнішим. Спортзал працює щодня з 7:00 до 22:00.
    `,
    worktime: '7:00 - 22:00',
    rules: {
      text: 'правила відвідування',
      link: '#'
    },
    images: [
      'slide-3', 'slide-2', 'slide-1'
    ],
  };

section.gym
  div.gym__container.container
    div.gym__top
      h2.title-inner.gym__title
        | !{gym.title}

      if gym.images.length > 0
        +slider('gym')
          for image in gym.images
            +slide('gym')
              +img('gym', '', image, gym.title)

        +pagination('gym')

    div.gym__bottom
      if gym.description !== ''
        p.gym__description
          | !{gym.description}

      if gym.worktime !== ''
        +worktime('gym', '', gym.worktime)

      div.gym__buttons
        if gym.rules && gym.rules.text && gym.rules.text !== '' && gym.rules.link && gym.rules.link !== ''
          a(class="gym__button button" href="" + gym.rules.link)
            span
              | !{gym.rules.text}


